import { Root, Label, Prefix, Field, PasswordField, ErrorMessages, PhoneField } from "./_ui";

export const Input = {
  Root,
  Label,
  Prefix,
  Field,
  PasswordField, 
  ErrorMessages,
  PhoneField
};
